# Core ADK runtime
google-adk>=2.1.0
mcp>=1.0.0           # Kaggle MCP toolset (StdioServerParameters); optional extra in ADK 2.x

# Persistent sessions (DatabaseSessionService). ADK builds an *async* SQLAlchemy
# engine, so SQLite needs the aiosqlite driver (sqlite+aiosqlite://) + greenlet.
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
greenlet>=3.0.0

# Data handling
pandas>=2.2.0
pyarrow>=15.0.0       # required for parquet serialization
openpyxl>=3.1.0       # Excel (.xlsx) support
xlrd>=2.0.0           # Excel (.xls) support

# EDA & visualization (M3) — in-agent stats + deterministic charts (plot_dataset).
# matplotlib uses the headless Agg backend; scipy powers skew/kurtosis + ANOVA.
matplotlib>=3.8.0
scipy>=1.11.0

# Optional: fuzzy deduplication
rapidfuzz>=3.6.0

# Environment & configuration
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Pydantic v2 (schemas layer)
pydantic>=2.11.0
